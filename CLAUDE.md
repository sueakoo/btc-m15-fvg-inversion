# BTC M15 FVG Inversion Algorithm — Project Context

## Что это за проект

Веб-приложение (standalone HTML + JS, без бэкенда) для анализа торговой формации "инверсия FVG" на таймфрейме M15 фьючерсов BTCUSDT Perpetual (Binance).

**Цель:** пользователь находит инверсию на графике, вставляет данные свечей в приложение, получает детерминированный отчёт о качестве инверсии — без субъективности.

**НЕ является:** торговым советником, сигнальным ботом, авто-трейдером.

## GitHub репозиторий

`https://github.com/sueakoo/btc-m15-fvg-inversion` (приватный)
Владелец: sueakoo
Локальная папка: `/Users/motkazver/btc-m15-fvg-inversion/`

## Платформа

- Standalone web app: один HTML файл + один JS файл + один CSS файл
- Работает в браузере (Safari, Chrome) на Mac, iPad, iPhone
- Деплой: GitHub Pages
- Никаких серверов, никакой установки
- Будущее: интеграция Claude API для AI-комментариев

## Ключевые концепции

### Что такое инверсия FVG
- FVG (Fair Value Gap / Имбаланс) = трёхсвечная формация n-1, n, n+1
  - Медвежий FVG: low(n-1) > high(n+1). upper_fvg = low(n-1), lower_fvg = high(n+1)
  - Бычий FVG: high(n-1) < low(n+1). upper_fvg = low(n+1), lower_fvg = high(n-1)
- Инверсия лонг: свеча закрывается телом выше upper_fvg медвежьего FVG (close > upper_fvg)
- Инверсия шорт: свеча закрывается телом ниже lower_fvg бычьего FVG (close < lower_fvg)
- Если close вышел за границу хотя бы на 1 пункт = факт инверсии
- Хвост за границей НЕ считается инверсией

### Ключевые окна анализа
- **Pivot** = разворотная свеча (экстремум перед движением к FVG)
- **OI_window** = Pivot → свеча инверсии включительно (Pivot ВХОДИТ в окно).
  Оговорка: отрицательный `doi_pct` самой свечи Pivot не учитывается в `unload_oi` — это разгрузка чужих позиций из предыдущего движения, не нашего сетапа. Положительный `doi_pct` Pivot в `gross_oi` учитывается.
- **FVG_overlap_window** = только свечи фактического перекрытия FVG
- **H1_window** = H1 свечи, покрывающие тот же период (следующая M15 после Pivot → инверсия)

### H1 данные
- H1 используется для ликвидаций и верификации (M15 ликвидации приходят с пропусками)
- Берутся все H1 свечи, чей период пересекается с M15 окном
- Незакрытая H1 берётся как snapshot (данные на текущий момент)
- Количество H1 свечей: переменное, обычно 1-4

### Очистка пивотного часа от снятия

> **Статус на 01.08.2026:** механизм сохранён, но блок 9 им больше не пользуется — он перешёл на ликвидации свечи инверсии. Очистка теперь нужна только для справочного показателя «ликвидации за окно», который выводится в отчёт. Оставлен, потому что делает эту цифру честной и уже проверен.

**Проблема.** Pivot на инверсии — это всегда снятие ликвидности, а снятие всегда даёт ликвидации. Часовая свеча, внутри которой лежит Pivot, захватывает их целиком, и ликвидации сетапа становятся неотличимы от ликвидаций снятия. На 18 сохранённых сетапах час Pivot попадал в окно **в 15 случаях из 18** — чистыми остаются только те, где Pivot пришёлся ровно на :45 и его час обрывается перед началом окна.

**Основание для решения.** Часовая свеча строго равна сумме своих четырёх M15-свечей — проверено на данных (по шортам совпадение до доллара). Значит вклад пивотного часа в окно можно вычислить двумя путями.

**Механизм.**
1. Находим H1-свечу, внутри которой лежит Pivot. Нет такой в окне — чистить нечего, всё как раньше.
2. Считаем её вклад в окно:
   - **Путь A, прямое сложение** (основной) — сумма M15-свечей этого часа, попавших в окно. Не зависит ни от округления H1, ни от полноты вставки слева.
   - **Путь B, вычитание** (запасной) — час минус M15-свечи до начала окна. Наследует ошибку округления H1 (наблюдаемая ~0.15%), поэтому применяется только когда A недоступен, и только при доказанной полноте: все свечи от начала часа до Pivot включительно должны присутствовать (проверяется по сетке времени).
   - **Доступны оба** — считаем оба и сверяем. Расхождение больше **2%** от суммы ликвидаций часа → флаг `h1_liq_mismatch`, данные не сходятся между собой. Берём всё равно путь A.
3. Остальные H1-свечи окна берутся как есть — они целиком внутри.
4. Объём чистится тем же способом. Обязательно: почистить ликвидации и не почистить объём — значит испортить долю.

**Пустое значение ликвидаций — это НЕ ноль.** Проверено вычитанием: у часовой свечи 22:00 лонги 765 070, а сумма её M15-свечей с данными — 575 920; разница 189 150 скрыта в двух пропусках. Фид отдаёт даже 63 доллара ликвидаций, значит мелкие суммы он не глотает. Свеча считается пригодной только когда есть обе стороны ликвидаций и объём.

**Отказ.** Наступает только когда пропуски с обеих сторон — и в свечах до окна, и в свечах внутри часа. Тогда:
- `h1_liqshare_pct` и `h1_limb_pct` = `null`, блок 9 выставляет балл только по оси OI;
- в отчёт идёт «Ликвидации не учтены: час, содержащий Pivot, не удалось отделить от снятия»;
- в сетап пишутся `h1_liq_clean: false` и `null` вместо сумм — «посчитать не смогли» не должно выглядеть как «ликвидаций не было».

**Что осталось не почищенным (осознанно).**
- *Хвост после инверсии.* Последняя H1-свеча тянется до конца часа и при разборе истории захватывает время после инверсии. При живом анализе свеча не закрыта и хвоста нет. Не чиним.
- *Ось OI на H1.* Вопрос снят: блок 9 больше не использует `h1_doi_pct`, он перешёл на `net_oi` — точный прирост за окно от Pivot до инверсии.

**Следствие для калибровки.** После очистки ликвидации у большинства сетапов уменьшаются. Наблюдения, отложенные «до 40 сетапов» — направление limb и потолки в верхних полосах — надо считать заново: старые 18 сетапов для этих вопросов не годятся, они собраны на неочищенных данных.

### Edge case: single_candle_inversion
- Если FVG перекрыта одной свечой (она же свеча инверсии): Re-Auction анализирует эту свечу
- Флаг `single_candle_inversion = true` выводится в отчёте

## Формат входных данных

Пользователь вставляет текстовые блоки вида:
```
ts: 15.06.2026 17:15
exchange: Binance
symbol: BTCUSDT Perp
tf: 15m
open: 75635.60
high: 75680.10
...
doi_pct: 0.14%
implied_price: 75653.00
avg_trade_buy: 0.03
avg_trade_sell: 0.06
```

Несколько свечей подряд. M15 и H1 вставляются раздельно в разные поля.
Парсер разбивает по `ts:`, определяет TF по полю `tf:`.

### Единицы данных
- `buy_volume` / `sell_volume` = BTC (базовый актив)
- `volume` = USDT (quote volume)
- `oi_open` / `oi_close` = BTC/контракты (не USD)
- `liq_long` / `liq_short` = USDT (ликвидации)
- `doi_pct` = изменение OI в % (ΔOI / OI_prev × 100)
- `implied_price` = OI-взвешенная имплицитная цена (из X-RAY, не вычисляем)
- `cvd_pct` = кумулятивная дельта в % от объёма
- `liqshare_pct` = доля ликвидаций от объёма (в %)
- `limb_pct` = дисбаланс ликвидаций (какая сторона преобладает)
- `oe` = OE = doi_pct / ABS(Δ% цены) — восстанавливается со знаком при разборе, см. ниже

### Нормализация полей X-RAY при разборе (js/parser.js)

Три поля приходят в выгрузке в виде, которому нельзя доверять напрямую. Приводятся к нашей конвенции **сразу на входе**, в `normalizeXrayFields()`, чтобы дальше по всему коду была одна шкала:

| Поле | Что приходит | Что делаем |
|---|---|---|
| `limb_pct` | X-RAY считает `(short − long)/сумма`, то есть **минус = вынесло лонгов** | инвертируем знак: у нас **плюс = вынесло лонгов** |
| `tilt_pct` | перекос «продавец относительно покупателя» (колонка `Tilt%`), что противоречит имени поля | считаем сами из сырых объёмов и числа сделок: `(buy_volume/buy_trades) / (sell_volume/sell_trades) − 1`. **Плюс = крупнее заходил покупатель.** `avg_trade_*` не используем — они округлены до 2 знаков и на мелких значениях схлопываются в ноль |
| `oe` | модуль (колонка `oe_modul`), сторона потеряна | считаем сами со знаком: `doi_pct / \|Δ% цены\|` |

Ещё одна ловушка, кода не требует: **`oipos` — это доля 0…1**, а в выгрузке к ней дописан знак процента. `oipos: 0,08%` означает «OI закрылся на 8% от своего диапазона», а не 0.08%.

### Правило: что берём готовым, что считаем сами

**По одной свече — берём готовым из X-RAY. По окну из нескольких свечей — считаем сами.**

Причина арифметическая: проценты, посчитанные от разных знаменателей, складывать и усреднять нельзя. Свеча с ликвидациями на сто долларов и свеча на миллион дадут одинаковый вес.

- **Считаем сами:** `limb` и `liqshare` по окну (сложить `liq_long` и `liq_short` по всем свечам, потом взять отношение), `oe`, `tilt`.
- **Берём готовым:** `cvd_pct`, `cvd_small`, `dpx` / `price_vs_delta`, `ratio_stable`, `oi_set` / `oi_counter` / `oi_unload` / `oi_in_sens`, `oipos`, `oi_path`, `dominant_reject`, `liq_squeeze`, `implied_price`, `body_pct`, `clv_pct`, хвосты.

### Словарь полей X-RAY (формулы из таблицы)

Формулы достаются экспортом таблицы в xlsx — через обычное чтение API видны только значения. Сборщик выгрузки — функция `makeZeroCalcForSelection` в `zero-calc.gs`.

- `cvd_pct` = `ABV_Delta / (ABV_Buy + ABV_Sell) × 100`. Реальный диапазон значений — от 1.5% до 44%.
- `cvd_small` = `|cvd_pct| < 1`
- `dpx` = `знак(Δцены) × знак(cvd)`; 1 = match, −1 = дивергенция
- `oi_counter` = дивергенция цены и потока **И** `ΔOI% > T_встречный`. Смысл: позицию набирают против видимого движения цены
- `dominant_reject` — шесть градаций, сверху вниз: bull_Ideal (нижний хвост ≥ 3× тела, верхний ≤ 10, CLV ≥ 85); bear_Ideal (зеркально, CLV ≤ 15); bull_Valid (≥ 2× тела, верхний ≤ 25, CLV ≥ 75); bear_Valid (зеркально, CLV ≤ 25); bull_Loose (≥ 1.5× тела, CLV ≥ 65, верхний ≤ половины нижнего); bear_Loose (зеркально, CLV ≤ 35)
- `oipos` = `(oi_close − oi_low)/(oi_high − oi_low)`, обрезано в 0…1 — аналог CLV для открытого интереса
- `oi_path` — сравнение `|OI_H − OI_O|` против `|OI_O − OI_L|`: куда OI дальше уходил **внутри свечи**, а не многосвечной тренд
- `ratio_stable` = `знак(cvd_pct) = знак(Trades_Delta)`
- `r` = `|ΔOI%| / POROG`; `ε` = `0.33 × POROG`; `oi_in_sens` = `|ΔOI%| ≤ POROG`
- `oi_set` = `ΔOI% > T_набор`; `oi_unload` = `ΔOI% ≤ T_разгрузка`
- `liq_squeeze` = `liqshare > 0.3%`, уровень warning — 0.2%
- `liqshare_pct` = `(liq_long + liq_short) / volume × 100` — формула идентична нашей

**Система порогов X-RAY двухуровневая:** `POROG` — граница шума, своя на актив и ТФ (BTC: 15m — 0.2%, 1h — 0.4%); `T_набор` / `T_встречный` / `T_разгрузка` = `SENS × коэффициент` (15m: 0.40 / 0.50 / −0.45; 1h: 0.50 / 0.60 / −0.55).

---

## Архитектура алгоритма — 8 блоков + 100 баллов

Блок 7 (Aggression/CVD) поглощён блоком 4. Итого 8 аналитических блоков.

### Блоки и веса (финальные, утверждены):
| # | Блок | Баллы |
|---|------|-------|
| 0 | Detection (технический, без баллов) | — |
| 1 | Energy / Энергоёмкость | 14 |
| 2 | OI Placement / Размещение OI | 11 |
| 3 | OI Retention / Сохранение OI | 13 |
| 4 | Механика захвата (бывшие блоки 4+7) | 16 |
| 5 | Re-Auction / Повторный аукцион | 13 |
| 6 | Geometry / Геометрия | 8 |
| 8 | Skew | 7 |
| 9 | H1 Snapshot | 18 |
| **Итого** | | **100** |

### Вердикты по итоговому баллу:
- 76–100: Сильный сетап
- 60–75: Рабочий сетап
- 45–59: Стандартная инверсия
- 30–44: Ослабленная инверсия
- < 30 или стоп-флаг: Не брать

### Стоп-флаги — любой = "НЕ БРАТЬ":
1. Пустая инверсия (нет OI + нет объёма + H1 не подтверждает)
2. Слабый OI без передачи риска (retention < 0.15, нет объёмной структуры)
3. Пустое FVG + слабый общий ресурс
4. Аномальный Skew: skew_depth > 1.20 ИЛИ implied_price < pivot_low (для лонга)
5. ~~H1 вынос стопов без живого интереса~~ — **вето снято на переходный период**: измерение блока 9 переопределено, прежняя калибровка недействительна. Балл блок ставит, зарубить сетап не может. Вернуть после накопления статистики.
6. Все основные блоки одновременно провалены
7. **Системная пустота**: Energy ≤ 4 И Retention ≤ 4 И Re-Auction ≤ 3 И H1 ≤ 3
8. **Механика захвата провалена**: Блок 4 ≤ 6 при любом общем балле.
   Исключение — сценарий `no_cvd`: там низкий балл означает отсутствие данных, а не провал механики, и вето не применяется.
   Основание: на 18 сетапах в эту группу попали три, и все три — стопы; ни один тейк не опускался ниже 11 баллов по блоку 4.

Плюс два флага, добавленных в коде и не входивших в исходный список: «Тройная слабость при высоком балле» и «FVG пробита насквозь при создании».

### Правила минимального порога:
- Если Energy ≤ 4 И Retention ≤ 4 → красный флаг "Пустой OI-ресурс" (не стоп, но в отчёт)
- Если выше + Re-Auction ≤ 3 + H1 ≤ 3 → Стоп-флаг #7

---

## Ключевые метрики

```
gross_oi = сумма max(doi_pct, 0) внутри OI_window
unload_oi = сумма min(doi_pct, 0) внутри OI_window
net_oi = gross_oi + unload_oi
retention_ratio = net_oi / gross_oi

# Блок 2 — ценовые зоны OI Placement (принадлежность свечи определяется по CLOSE, не по high/low):
share_below = Σ(max(doi_pct,0) для свечей где close < lower_fvg [лонг] / close > upper_fvg [шорт]) / gross_oi
share_fvg   = Σ(max(doi_pct,0) для свечей где lower_fvg ≤ close ≤ upper_fvg) / gross_oi
share_inv   = max(doi_pct[inversion_candle], 0) / gross_oi

fvg_volume_share = volume_FVG_overlap / post_pivot_volume
final_volume_share = volume_inversion_candle / post_pivot_volume

imb_range_pct = (upper_fvg - lower_fvg) / mid_fvg × 100

skew_depth = (close_inv - implied_price) / (upper_fvg - lower_fvg)  [лонг]
           = (implied_price - close_inv) / (upper_fvg - lower_fvg)  [шорт]

# H1 агрегация:
h1_doi_pct = Σ(doi_pct) по всем H1 свечам окна
h1_liqshare_pct = (Σliq_long + Σ|liq_short|) / Σvolume × 100
h1_limb_pct = (Σliq_long − Σ|liq_short|) / (Σliq_long + Σ|liq_short|) × 100
h1_cvd_sign = знак(Σ cvd_pct)
```

---

## Логика блоков — утверждённые решения

### Блок 0 — Detection

**Pivot (авто-определение):**
- Лонг: min LOW среди свечей где low < lower_fvg (ниже зоны FVG), до инверсии. При равных → берём более позднюю.
- Шорт: max HIGH среди свечей где high > upper_fvg (выше зоны FVG), до инверсии. При равных → берём более позднюю.

**Несколько FVG в данных:**
- Алгоритм находит первую инверсию → анализирует только её, остальное игнорирует.
- Если два FVG одного направления в одном движении (без разворота между ними) → объединяем в одну зону: upper_fvg = max(upper_fvg), lower_fvg = min(lower_fvg). Один отчёт.

**FVG размеры (imb_range_pct):**
- < 0.15% = малый FVG
- 0.15–0.40% = средний FVG
- > 0.40% = большой FVG

---

### Блок 1 — Energy (14 pts)

**Входные данные:** gross_oi, total_volume, fvg_volume_share, pivot_volume_share, imb_range_pct.
**НЕ использует:** net_oi, retention_ratio, unload_oi (это зона Блока 3).

**Отвечает на вопрос:** был ли ресурс набран и участвовал ли в зоне?

Таблица баллов (0–14) — из MASTER документа, с удалённой строкой "скрытая передача риска":
```
14: net_oi >= +0.40% + выраженное объёмное участие внутри FVG/финальной свече
13: net_oi >= +0.40% + объёмная структура нормальная
12: net_oi +0.20–0.39% + выраженное объёмное участие
11: net_oi +0.20–0.39% + нормальная объёмная структура
10: net_oi >= +0.40% + объём без усиления
9:  net_oi +0.20–0.39% + объём распределён стандартно
8:  net_oi +0.20–0.39% + объём слабее, FVG малый/средний
7:  net_oi < +0.20%, gross_oi >= +0.40%, объём частично показывает участие
6:  net_oi < +0.20%, gross_oi +0.20–0.39%, хотя бы один признак участия
5:  net_oi +0.20–0.39%, объём без усиления, FVG большой
4:  net_oi < +0.20%, gross_oi +0.20–0.39%, объём без участия
3:  net_oi < +0.20%, gross_oi < +0.20%, без участия, FVG малый
2:  net_oi < +0.20%, gross_oi < +0.20%, без участия, FVG средний
1:  net_oi < +0.20%, gross_oi < +0.20%, без участия, FVG большой
0:  нет нового OI, нет объёмного участия
```
*(Строка 12 исходного документа "скрытая передача риска" удалена — это зона Блока 3)*

---

### Блок 2 — OI Placement (11 pts)

**Отвечает на вопрос:** где сосредоточен новый OI относительно FVG — у кого есть подушка перед тестом, кто сидит на максимуме.

**Логика:** ценовые зоны (не счётчик свечей). Принадлежность свечи к зоне определяется **по close (телу)**, а не по хвостам — та же логика, что и в определении инверсии:
- **Zone_below**: close < lower_fvg (лонг) / close > upper_fvg (шорт) — свеча закрылась ДО зоны. Хвост мог зайти в FVG, это не меняет принадлежность.
- **Zone_fvg**: lower_fvg ≤ close ≤ upper_fvg — свеча закрылась внутри зоны.
- **Zone_inv**: свеча инверсии (close > upper_fvg для лонга, close < lower_fvg для шорта).

```
share_below = Σ(max(doi_pct,0) в Zone_below) / gross_oi
share_fvg   = Σ(max(doi_pct,0) в Zone_fvg)   / gross_oi
share_inv   = max(doi_pct[inv], 0)             / gross_oi
```

**Таблица баллов:**
| share_below + share_fvg | Уточнение | Балл |
|------------------------|-----------|------|
| ≥ 0.80 | share_fvg ≥ 0.40 | 11 |
| ≥ 0.80 | share_fvg < 0.40 | 10 |
| 0.60–0.79 | share_fvg ≥ 0.30 | 9 |
| 0.60–0.79 | share_fvg < 0.30 | 8 |
| 0.40–0.59 | share_fvg ≥ 0.20 | 6 |
| 0.40–0.59 | share_fvg < 0.20 | 5 |
| 0.20–0.39 | share_fvg ≥ 0.10 | 4 |
| 0.20–0.39 | share_fvg < 0.10 | 3 |
| < 0.20 | share_fvg ≥ 0.05 | 2 |
| < 0.20 | share_fvg < 0.05 | 1 |
| gross_oi < 0.20% | cap max 4 | ≤ 4 |
| OI_window ≤ 3 свечей | cap max 8 | ≤ 8 |
| gross_oi = 0 | — | 0 |

---

### Блок 3 — OI Retention (13 pts)

**Отвечает на вопрос:** сколько живого OI осталось после завершения инверсии — есть ли кому защищать зону при тесте.

**Входные данные:** net_oi, retention_ratio, fvg_volume_share.
**НЕ использует:** gross_oi как первичный (только контекст в передаче риска).
**НЕ использует:** паттерны порядка событий (нельзя определить сторону OI-снятий без структурного слома).

**Передача риска** (отдельный сценарий):
Условие: net_oi < 0.20% + gross_oi ≥ 0.40% + fvg_volume_share ≥ 35%
→ Высокая активность в зоне при слабом чистом OI. Вероятная смена позиции. Прямого подтверждения нет — отмечается в комментарии.

**Логика: 4 сценария по ТАЙМИНГУ снятий OI.**
Старая 2D-матрица (net_oi × retention_ratio) отменена — важно не только сколько OI ушло, но и ГДЕ оно ушло: до зоны FVG или уже внутри неё.

Вспомогательные величины:
```
exit_pre_fvg = |Σ отрицательных doi_pct на свечах ДО зоны FVG|
exit_in_fvg  = |Σ отрицательных doi_pct на свечах ВНУТРИ зоны FVG|
hasExits     = (exit_pre_fvg + exit_in_fvg) > 0.03
buildInFvg   = share_fvg >= 0.50
```

| Сценарий | Условие | Что произошло |
|----------|---------|---------------|
| `no_exit` | нет снятий | Набор дошёл до инверсии целиком, ресурс полный |
| `fvg_fvg` | набор в зоне + снятие преимущественно в зоне | Бой внутри имбаланса: и набирали, и снимали в зоне |
| `pre_fvg` | набор до зоны + снятие в зоне | Позиции вышли при входе цены в FVG |
| `early_exit` | набор до зоны + снятие до зоны | Разгрузились ещё на подходе, до имбаланса |

**Баллы (net_oi × сценарий):**
| net_oi | no_exit | fvg_fvg | pre_fvg | early_exit |
|---|---|---|---|---|
| ≥ 0.40% | 13 | 13 | 11 | 9 |
| 0.20–0.39% | 11 | 11 | 9 | 7 |
| 0.10–0.19% | 7 | 6 | 6 | 4 |
| 0.03–0.09% | 4 | 3 | 3 | 2 |
| < 0.03% | 4 | 1 | 3 | 2 |
| gross_oi = 0 | 0 | 0 | 0 | 0 |

Сценарий `fvg_fvg` при сильно отрицательном OI дополнительно запускает стоп-флаг «FVG пробита насквозь при создании» (см. стоп-флаги).

---

### Блок 4 — Механика захвата (16 pts)

*(Объединение бывших блоков 4 и 7. Блок 7 как отдельный не существует.)*

**Отвечает на вопрос:** каким механизмом цена прошла через FVG и инвертировала зону.

**Вспомогательные метрики (только в комментарий, не в скоринг):** OE, avg_trade_buy/sell, ER.

**Классификация тела свечи (связка body_pct + CVD):**
- body_pct ≥ 50% + clv в сторону инверсии ≥ 60% = сильное направленное тело
- body_pct < 35% + сильный CVD в сторону инверсии = "Инверсия через сопротивление"
- body_pct < 35% + слабый CVD = "Движение без позиции"

**Сценарии** (проверяются в этом порядке, первое совпадение выигрывает):
| Сценарий | Что произошло | Балл |
|---------|---------------|------|
| Нет данных по CVD | Отсутствует знак ИЛИ величина CVD. Оценка только по OI, нейтральная. Под стоп-флаг «механика провалена» НЕ попадает. | 3–5 |
| Инверсия через сопротивление | CVD сильный в сторону инверсии, тело слабое — встречная стена была, но цена прошла. | 9–11 |
| Рыночная инициатива | CVD и цена в одну сторону, OI растёт. Прямое направленное давление. | 11–13 |
| Возможный хедж | OI растёт, тело < 20%, CVD нейтральный. | 2 |
| Лимитное поглощение | CVD против цены или нейтральный, цена выросла, OI набирается. Крупный лимит поглощал продажи. | 13–16 |
| Движение без позиции | Цена прошла FVG, CVD слабый, OI не набирается. | 4–6 |

**Пороги CVD.** Единственный порог — готовый флаг `cvd_small` из выгрузки, то есть `|cvd_pct| < 1%`:
- «сильный CVD» = `cvd_small` снят,
- «нейтральный CVD» = `cvd_small` поднят.

Собственные пороги 0.30 и 0.10 удалены как избыточные: при снятом флаге `|cvd_pct|` и так ≥ 1%, проверка всегда истинна. Реальные значения `cvd_pct` — от 1.5% до 44%.

**Сценарий «Пустое движение» удалён.** Его условие `|cvd_pct| < 0.10` при живых данных не выполняется никогда, а срабатывало оно только на пропущенных данных — то есть отсутствие данных выдавалось за отсутствие потока. Теперь такие свечи уходят в ветку «нет данных». Случай «нет потока и нет набора OI» покрыт сценарием «Движение без позиции» (4 балла) и стоп-флагом №8.

**Что реально различает блок** (по 18 сетапам): 13 баллов → 9 тейков из 9; 11 баллов → 3 из 6; ≤ 6 → 0 из 3. Разделение идёт по признаку «сильное направленное тело», не по CVD.

---

### Блок 5 — Re-Auction (13 pts)

**Отвечает на вопрос:** как FVG зона была принята в процессе прохождения — был ли реальный аукцион внутри зоны.

**Особые случаи:**

**single_candle_inversion (одна свеча закрыла весь FVG):**
- fvg_volume_share = null (не считается — 100% по определению)
- Анализируем: doi_pct этой свечи > 0? CVD в нужную сторону?
- Максимальный балл: **7/13**
- Вывод: "Инверсия однослайновая — объёмное распределение нерелевантно"

**candle_count_fvg = 1:** max 7/13, CVD бинарный (подтверждает / не подтверждает)
**candle_count_fvg = 2:** max 9/13, CVD бинарный (убираем "mixed")
**candle_count_fvg ≥ 3:** стандартная логика, CVD может быть mixed

**fvg_volume_share порог:**
- Стандартно (≥ 3 свечей в FVG): ≥ 50% = выраженное участие
- При candle_count_fvg ≤ 2: порог поднят до **≥ 70%** = выраженное участие

---

### Блок 6 — Geometry (8 pts)

**Отвечает на вопрос:** насколько чисто выглядит геометрия движения — риск-сигнал, не показатель качества.

- Анализ начинается со свечи ПОСЛЕ Pivot (Pivot исключён)
- directional_tail_risk_avg > 50% → **минимум 2 балла** (не 0), + флаг "Кульминационный хвост: повышен риск глубокого теста"
- Слабая геометрия при сильном OI/flow → флаг, не инвалидация

---

### Блок 8 — Skew (7 pts)

**Отвечает на вопрос:** куда тянет алгоритмическое позиционирование — поддерживает ли implied_price инверсию или работает против.

**Формула:**
```
skew_depth = (close_inv - implied_price) / (upper_fvg - lower_fvg)  [лонг]
skew_depth = (implied_price - close_inv) / (upper_fvg - lower_fvg)  [шорт]
```

implied_price берётся из X-RAY как готовое поле, не вычисляется.

**Шкала:**
| skew_depth | Балл |
|------------|------|
| ≤ 0 | 7 |
| 0–0.15 | 6 |
| 0.15–0.33 | 5 |
| 0.33–0.50 | 4 |
| 0.50–0.75 | 3 |
| 0.75–1.00 | 2 |
| 1.00–1.20 | 1 |
| > 1.20 | 0 + СТОП-ФЛАГ |

**Дополнительный стоп-флаг:** implied_price < pivot_low (для лонга) → стоп независимо от skew_depth.

**Skew trend:** только флаги/комментарий, НЕ влияет на балл.

---

### Блок 9 — Ликвидации и позиция при пересечении зоны (18 pts)

**Отвечает на вопрос:** чем сопровождалось пересечение зоны — был ли вынос стопов и набиралась ли позиция.

**Почему блок переопределён (01.08.2026).** Раньше назывался «H1 Snapshot» и заявлял проверку через старший таймфрейм. Такой проверки не существует: ликвидации, OI, объём и CVD на H1 — это ровно сумма пятнадцатиминуток внутри часа, проверено на данных, сходится до доллара. Час не показывает ничего нового, он лишь агрегирует то же самое по более грубому и смещённому окну.

Отсюда все прежние болезни блока. В окно попадал час, содержащий Pivot, а Pivot на инверсии — это всегда снятие ликвидности, то есть гарантированные ликвидации. В результате ликвидации снятия выдавались за ликвидации зоны, а прирост OI получал неверный знак: на разобранном сетапе H1 показывал −0.46%, тогда как реальный прирост за окно составлял +0.42%.

**Новые оси:**
- **Ось 1 — ликвидации на свече инверсии.** Момент пересечения зоны, определён структурно, без единого порога и без привязки к расстояниям. Ликвидации снятия попасть в неё физически не могут.
- **Ось 2 — прирост OI за окно** (`net_oi`, от Pivot до инверсии). Точная величина, уже считается блоками 1 и 3.

**H1 остаётся, но только как источник данных** — через сверку с часом закрываются пропуски в M15.

**Восстановление пропусков в ликвидациях.** Прочерк в данных M15 — это НЕ ноль. Проверено на 17 полных часах: в 11 случаях суммы M15 сошлись с часом (значит пропуски были нулями), а в 6 случаях за прочерками прятались реальные суммы — до 195 тысяч в одном часе, всего 451 тысяча по выборке.

Механизм восстановления для свечи инверсии:
1. Значение есть в самой свече — берём напрямую (`inv_liq_method: 'direct'`).
2. Пропуск, но час по этой стороне пуст — значит за весь час ликвидаций не было, на свече ноль. Фид отдаёт даже 63 доллара, поэтому пустой час = пустой час.
3. Пропуск, час известен и все остальные свечи часа известны — недостающее вычисляется точно как разность (`'recovered'`).
4. Пропуск и в часе несколько неизвестных свечей — точное значение недоступно, но известна **верхняя граница**: весь нераспределённый остаток часа. Блок считает балл по одной оси OI и пишет в отчёт «ликвидации не определены» с указанием границы.

**Полосы ликвидаций перенесены на реальную шкалу.** Прежние границы 2 / 5 / 10% были недостижимы: ликвидации составляют сотые и десятые доли процента от оборота (по 18 сетапам диапазон 0.005–0.555%, медиана 0.13%). Из-за этого девять сценариев из тринадцати не запускались ни разу, limb не влиял на балл, а стоп-флаг №5 не мог сработать в принципе.

Новые границы взяты из калибровки самого X-RAY (лист «пороги»): **0.2% — warning, 0.3% — squeeze**.

**Матрица сценариев:**
| ликвидации свечи инверсии | OI за окно (net_oi) | limb | Сценарий | Балл |
|------------|-------------------|------|----------|------|
| < 0.1% | ≥ 0.20% | любое | Чистый рост: новый OI без стопов | 18 |
| < 0.1% | 0–0.19% | любое | Слабый рост: OI почти не появился | 13 |
| < 0.1% | < 0 | любое | OI уходит, H1 не поддерживает | 5 |
| 0.1–0.2% | ≥ 0.20% | против инверсии | Зачистка позиций в направлении + OI вошёл | 14 |
| 0.1–0.2% | ≥ 0.20% | в сторону инверсии | Встряска противоположной стороны + OI вошёл | 11 |
| 0.1–0.2% | < 0.20% | любое | H1 нейтрален, подтверждения нет | 7 |
| 0.2–0.3% | ≥ 0.20% | против инверсии | Выброс позиций в направлении поглощён + OI вошёл | 13 |
| 0.2–0.3% | ≥ 0.20% | в сторону инверсии | Ликвидации противоположной стороны + OI вошёл | 11 |
| 0.2–0.3% | 0–0.19% | любое | Ликвидации без роста OI | 7 |
| 0.2–0.3% | < 0 | любое | Ликвидации + OI снижается | 4 |
| ≥ 0.3% | ≥ 0.20% | любое | Массовые ликвидации поглощены + OI набирается | 14 |
| ≥ 0.3% | 0–0.19% | любое | Высокая доля стопов, OI не растёт | 2 |
| ≥ 0.3% | < 0 | любое | Вынос стопов без живого интереса — СТОП | 0 |

CVD как уточнение: ±2 при пограничных значениях. Пограничные зоны — ±0.025 вокруг каждой границы полос, плюс doi в диапазоне 0.17–0.25%.

**Порог OI оставлен 0.20%.** Проверена альтернатива — порог шума POROG (для BTC H1 это 0.4%): она дала 80% отработки против 82% у текущего порога, то есть хуже.

**limb в баллы отдельно не вводится.** Данные намекают на направление, противоположное заложенному (когда выносило противоположную сторону — 78% отработки, когда наших — 56%), но выборка девять на девять слишком мала. Поле сохраняется в сетап, вернуться при 40+ сетапах.

**На заметку для будущей калибровки:** максимум 18 достижим только в нижней полосе, то есть замысел считает «чистый рост без ликвидаций» лучшим случаем, а данные намекают, что ликвидации отработке не мешают (при liqshare ≥ 0.2% отработка 80%, при меньшем — 62%). Пересмотреть потолки в верхних полосах при 40+ сетапах.

---

## Ожидаемый тест

Строится на основе: skew_depth + Re-Auction + Geometry + H1.
Уровни: Мелкий / Средний / Глубокий / К Pivot / Риск провала.
Всегда включает конкретную ценовую зону: "Тест ожидается в [lower_price – upper_price]".

---

## Комментарии — правила (применять при написании)

- "Типовая причина" = описывает что произошло на рынке (механику), не эхо метрик
- "Ожидаемый тест" = всегда с конкретными уровнями цен
- Убрать повторения "слабый OI не режет" из Блока 3
- Язык: трейдерский, без технического жаргона алгоритма

---

## Что НЕ входит в текущую версию

- Логика MVP (H4/D sweep, imbalance test, MACD, return) — следующий этап
- ML / AI автоматический анализ — следующий этап
- Сохранение истории сетапов — следующий этап

---

## Статус разработки

Обновлено: 01.08.2026

- [x] Концепция и алгоритм изучены
- [x] Алгоритм полностью пересмотрен и задокументирован (все блоки утверждены)
- [x] GitHub репо создано
- [x] Парсер входных данных (`js/parser.js`)
- [x] Detection блок (`js/detection.js`)
- [x] Метрики (`js/metrics.js`)
- [x] 8 аналитических блоков + стоп/красные флаги + ожидаемый тест (`js/scoring.js`)
- [x] Контекстуальный анализ MVP: снятие / имбаланс / OB / возврат / MACD / золотые сетапы (`js/scoring_mvp.js`)
- [x] UI (форма ввода + отчёт + сохранение сетапов в localStorage + экспорт JSON)
- [x] Деплой на GitHub Pages — https://sueakoo.github.io/btc-m15-fvg-inversion/

**Калибровка на 01.08.2026:** проверено на 18 сохранённых сетапах (12 тейков / 6 стопов). По вердиктам: «Сильный сетап» — 89% отработки, «Рабочий» — 50%, «Не брать» — 0%. Допущенные к работе после правок: 11 тейков / 3 стопа = 79% (было 69%).

Что ещё ждёт данных (вернуться при 40+ сетапах):
- направление limb (данные намекают на обратное заложенному, выборка 9 против 9);
- потолки в верхних полосах ликвидаций блока 9;
- пороги CVD в блоке 4 — для этого в сетап уже сохраняются `cvd_pct_inv`, `clv_pct_inv`, `doi_pct_inv`, `h1_liq_long`, `h1_liq_short`;
- стоп-флаг «FVG пробита насквозь» — одна ложная блокировка на 18 сетапов (#8 отработал в тейк).

**Следующий этап:** формация СМС (Хай-1 → Лой-1 → Хай-2 → пробой). Форма сбора тестового материала — `sms-log.html` + `sms-log.js`, к скорингу инверсий отношения не имеет, работает отдельной страницей.

---

## Открытые вопросы

**`OPEN_QUESTIONS.md`** — реестр непроверенных чисел и правил алгоритма. Читать перед любой правкой скоринга. Там записано, какие пороги стоят на слабом основании, при каком количестве сетапов их пересчитывать и какие поля для этого нужны.

**`BLOCK4_REDESIGN.md`** — переработка блоков 4 и 9, принятые решения и обоснования. В код не внесено.

---

## Документы-источники

- **M15 FVG Inversion Algorithm — MASTER** (Google Doc ID: `1xJTtKnxeoQFcE4esU_r-_RDjgyN3rCBG2sAijioUupg`) — исходный документ алгоритма (часть правок внесена только в CLAUDE.md)
- **btc-m15-scoring** (github.com/sueakoo/btc-m15-scoring) — MVP версия (GAS + Sheets)
- **Метрики таблица** (Google Sheets ID: `1EMOWiPbK7R093J6vr6LS9ViqwCxjRmoqJb_Ln7TPfPo`) — справочник по метрикам

---

## Пользователь

- Не технический (0 в разработке) — объяснять просто
- Торгует BTC perpetual futures
- Работает с X-RAY данными (кастомный инструмент)
- Использует Mac, iPad, iPhone
- Хочет приватность и простоту использования
- **Требование:** каждое изменение автоматически пушится в GitHub без напоминаний
